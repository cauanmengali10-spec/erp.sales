server:
  servlet:
    context-path: /faturamento/api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sales
    username: root
    password: faturarnota

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  swagger-ui:
    path: /swagger